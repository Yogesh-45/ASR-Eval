# Secrets
.env

# Python
venv/
.venv/
__pycache__/
*.pyc
*.pyo
.pytest_cache/
*.egg-info/

# OS / editor cruft
.DS_Store
*.swp
.~lock.*

# Raw customer call data (full transcripts + recording URLs) and the LLM
# extraction cache (embeds real transcript spans) -- shared under NDA for this
# assignment only (see README.md's "The data" section). Never commit.
data/

# Orphaned/stale report artifacts: predate the single-call alignment rewrite,
# not regenerated by any current script, not referenced by the current README.
reports_no_hints/
reports/wer_blind_spots_comparison.pdf

# Confidential assignment prompt and internal pre-work planning/status notes --
# not part of the deliverable; kept locally for personal reference only.
docs/Founding ML Engineer Assignment.docx
docs/asr_failure_analysis_implementation_plan.md
docs/implementation_status.md
docs/learning.txt

# Kept locally only, not pushed to the remote (removed on request).
docs/challenges_and_decisions.md
docs/reports_data_dictionary.md
